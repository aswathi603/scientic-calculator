# scientic-calculator

        import os
        import  math 
        def simple_sum(x,char,y):
            match char:
                  case '+':
                        return(x+y)
                  case '-':
                        return(x-y)
                  case '*':
                        return(x*y)
                  case '%':
                        return(x%y)
                  case '^':
                        return(x**y)
                  case '/':
                         return(x/y)

      # for sicentific calculater 
      def sicentific_calculater(choice,x):
          match choice:
                    case 1:
                
